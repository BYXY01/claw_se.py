# USER - who you serve, and what they prefer.

This file is the user's own space: stable preferences and profile facts about
who they are. The user edits it; read it and adapt your tone, defaults, and
behavior to it. One directive per entry, each with a metadata line.

<!-- observed: YYYY-MM-DD | status: active -->

- Prefer concise, actionable replies.
- Never guess an action the user did not request.

When a preference changes, mark the old entry `superseded` and rewrite the
active directive in place - never append a contradictory directive.

You may PROPOSE directives here when the user shares new preferences, but only
write them after the user agrees - never edit this file silently.
